/**
 * Nudm_UECM
 * Nudm Context Management Service.   © 2023, 3GPP Organizational Partners
 * (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 1.2.5
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "SmfRegistrationInfo.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

SmfRegistrationInfo::SmfRegistrationInfo() {}

void SmfRegistrationInfo::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool SmfRegistrationInfo::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool SmfRegistrationInfo::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success = true;
  const std::string _pathPrefix =
      pathPrefix.empty() ? "SmfRegistrationInfo" : pathPrefix;

  /* SmfRegistrationList */ {
    const std::vector<oai::_3gpp::model::SmfRegistration>& value =
        m_SmfRegistrationList;
    const std::string currentValuePath = _pathPrefix + ".smfRegistrationList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::SmfRegistration& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success =
            value.validate(msg, currentValuePath + ".smfRegistrationList") &&
            success;

        i++;
      }
    }
  }

  return success;
}

bool SmfRegistrationInfo::operator==(const SmfRegistrationInfo& rhs) const {
  return

      (getSmfRegistrationList() == rhs.getSmfRegistrationList())

          ;
}

bool SmfRegistrationInfo::operator!=(const SmfRegistrationInfo& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const SmfRegistrationInfo& o) {
  j                        = nlohmann::json();
  j["smfRegistrationList"] = o.m_SmfRegistrationList;
}

void from_json(const nlohmann::json& j, SmfRegistrationInfo& o) {
  j.at("smfRegistrationList").get_to(o.m_SmfRegistrationList);
}

std::vector<oai::_3gpp::model::SmfRegistration>
SmfRegistrationInfo::getSmfRegistrationList() const {
  return m_SmfRegistrationList;
}
void SmfRegistrationInfo::setSmfRegistrationList(
    std::vector<oai::_3gpp::model::SmfRegistration> const& value) {
  m_SmfRegistrationList = value;
}

}  // namespace oai::_3gpp::model
