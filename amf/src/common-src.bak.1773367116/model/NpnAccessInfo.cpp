/**
 * Namf_Communication
 * AMF Communication Service.   © 2023, 3GPP Organizational Partners (ARIB,
 * ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 1.2.4
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "NpnAccessInfo.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

NpnAccessInfo::NpnAccessInfo() {
  m_CellCagInfoIsSet = false;
}

void NpnAccessInfo::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool NpnAccessInfo::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool NpnAccessInfo::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success = true;
  const std::string _pathPrefix =
      pathPrefix.empty() ? "NpnAccessInfo" : pathPrefix;

  if (cellCagInfoIsSet()) {
    const std::vector<std::string>& value = m_CellCagInfo;
    const std::string currentValuePath    = _pathPrefix + ".cellCagInfo";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const std::string& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        i++;
      }
    }
  }

  return success;
}

bool NpnAccessInfo::operator==(const NpnAccessInfo& rhs) const {
  return

      ((!cellCagInfoIsSet() && !rhs.cellCagInfoIsSet()) ||
       (cellCagInfoIsSet() && rhs.cellCagInfoIsSet() &&
        getCellCagInfo() == rhs.getCellCagInfo()))

          ;
}

bool NpnAccessInfo::operator!=(const NpnAccessInfo& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const NpnAccessInfo& o) {
  j = nlohmann::json();
  if (o.cellCagInfoIsSet() || !o.m_CellCagInfo.empty())
    j["cellCagInfo"] = o.m_CellCagInfo;
}

void from_json(const nlohmann::json& j, NpnAccessInfo& o) {
  if (j.find("cellCagInfo") != j.end()) {
    j.at("cellCagInfo").get_to(o.m_CellCagInfo);
    o.m_CellCagInfoIsSet = true;
  }
}

std::vector<std::string> NpnAccessInfo::getCellCagInfo() const {
  return m_CellCagInfo;
}
void NpnAccessInfo::setCellCagInfo(std::vector<std::string> const& value) {
  m_CellCagInfo      = value;
  m_CellCagInfoIsSet = true;
}
bool NpnAccessInfo::cellCagInfoIsSet() const {
  return m_CellCagInfoIsSet;
}
void NpnAccessInfo::unsetCellCagInfo() {
  m_CellCagInfoIsSet = false;
}

}  // namespace oai::_3gpp::model
