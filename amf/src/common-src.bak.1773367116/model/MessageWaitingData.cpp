/**
 * Nudr_DataRepository API OpenAPI file
 * Unified Data Repository Service.   © 2023, 3GPP Organizational Partners
 * (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 2.2.4
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "MessageWaitingData.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

MessageWaitingData::MessageWaitingData() {
  m_MwdListIsSet = false;
}

void MessageWaitingData::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool MessageWaitingData::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool MessageWaitingData::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success = true;
  const std::string _pathPrefix =
      pathPrefix.empty() ? "MessageWaitingData" : pathPrefix;

  if (mwdListIsSet()) {
    const std::vector<oai::_3gpp::model::SmscData>& value = m_MwdList;
    const std::string currentValuePath = _pathPrefix + ".mwdList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::SmscData& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success = value.validate(msg, currentValuePath + ".mwdList") && success;

        i++;
      }
    }
  }

  return success;
}

bool MessageWaitingData::operator==(const MessageWaitingData& rhs) const {
  return

      ((!mwdListIsSet() && !rhs.mwdListIsSet()) ||
       (mwdListIsSet() && rhs.mwdListIsSet() &&
        getMwdList() == rhs.getMwdList()))

          ;
}

bool MessageWaitingData::operator!=(const MessageWaitingData& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const MessageWaitingData& o) {
  j = nlohmann::json();
  if (o.mwdListIsSet() || !o.m_MwdList.empty()) j["mwdList"] = o.m_MwdList;
}

void from_json(const nlohmann::json& j, MessageWaitingData& o) {
  if (j.find("mwdList") != j.end()) {
    j.at("mwdList").get_to(o.m_MwdList);
    o.m_MwdListIsSet = true;
  }
}

std::vector<oai::_3gpp::model::SmscData> MessageWaitingData::getMwdList()
    const {
  return m_MwdList;
}
void MessageWaitingData::setMwdList(
    std::vector<oai::_3gpp::model::SmscData> const& value) {
  m_MwdList      = value;
  m_MwdListIsSet = true;
}
bool MessageWaitingData::mwdListIsSet() const {
  return m_MwdListIsSet;
}
void MessageWaitingData::unsetMwdList() {
  m_MwdListIsSet = false;
}

}  // namespace oai::_3gpp::model
