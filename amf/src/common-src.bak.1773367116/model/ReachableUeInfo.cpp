/**
 * Namf_MT
 * AMF Mobile Terminated Service.   © 2023, 3GPP Organizational Partners (ARIB,
 * ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 1.2.3
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "ReachableUeInfo.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

ReachableUeInfo::ReachableUeInfo() {
  m_UserLocationIsSet = false;
}

void ReachableUeInfo::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool ReachableUeInfo::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool ReachableUeInfo::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success = true;
  const std::string _pathPrefix =
      pathPrefix.empty() ? "ReachableUeInfo" : pathPrefix;

  /* UeList */ {
    const std::vector<std::string>& value = m_UeList;
    const std::string currentValuePath    = _pathPrefix + ".ueList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const std::string& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        i++;
      }
    }
  }

  return success;
}

bool ReachableUeInfo::operator==(const ReachableUeInfo& rhs) const {
  return

      (getUeList() == rhs.getUeList()) &&

      ((!userLocationIsSet() && !rhs.userLocationIsSet()) ||
       (userLocationIsSet() && rhs.userLocationIsSet() &&
        getUserLocation() == rhs.getUserLocation()))

          ;
}

bool ReachableUeInfo::operator!=(const ReachableUeInfo& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const ReachableUeInfo& o) {
  j           = nlohmann::json();
  j["ueList"] = o.m_UeList;
  if (o.userLocationIsSet()) j["userLocation"] = o.m_UserLocation;
}

void from_json(const nlohmann::json& j, ReachableUeInfo& o) {
  j.at("ueList").get_to(o.m_UeList);
  if (j.find("userLocation") != j.end()) {
    j.at("userLocation").get_to(o.m_UserLocation);
    o.m_UserLocationIsSet = true;
  }
}

std::vector<std::string> ReachableUeInfo::getUeList() const {
  return m_UeList;
}
void ReachableUeInfo::setUeList(std::vector<std::string> const& value) {
  m_UeList = value;
}
oai::_3gpp::model::UserLocation ReachableUeInfo::getUserLocation() const {
  return m_UserLocation;
}
void ReachableUeInfo::setUserLocation(
    oai::_3gpp::model::UserLocation const& value) {
  m_UserLocation      = value;
  m_UserLocationIsSet = true;
}
bool ReachableUeInfo::userLocationIsSet() const {
  return m_UserLocationIsSet;
}
void ReachableUeInfo::unsetUserLocation() {
  m_UserLocationIsSet = false;
}

}  // namespace oai::_3gpp::model
