/**
 * Namf_Communication
 * AMF Communication Service.   © 2023, 3GPP Organizational Partners (ARIB,
 * ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 1.2.4
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "SliceLoadLevelInformation.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

SliceLoadLevelInformation::SliceLoadLevelInformation() {
  m_LoadLevelInformation = 0;
}

void SliceLoadLevelInformation::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool SliceLoadLevelInformation::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool SliceLoadLevelInformation::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success = true;
  const std::string _pathPrefix =
      pathPrefix.empty() ? "SliceLoadLevelInformation" : pathPrefix;

  /* Snssais */ {
    const std::vector<oai::_3gpp::model::Snssai>& value = m_Snssais;
    const std::string currentValuePath = _pathPrefix + ".snssais";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::Snssai& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success = value.validate(msg, currentValuePath + ".snssais") && success;

        i++;
      }
    }
  }

  return success;
}

bool SliceLoadLevelInformation::operator==(
    const SliceLoadLevelInformation& rhs) const {
  return

      (getLoadLevelInformation() == rhs.getLoadLevelInformation()) &&

      (getSnssais() == rhs.getSnssais())

          ;
}

bool SliceLoadLevelInformation::operator!=(
    const SliceLoadLevelInformation& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const SliceLoadLevelInformation& o) {
  j                         = nlohmann::json();
  j["loadLevelInformation"] = o.m_LoadLevelInformation;
  j["snssais"]              = o.m_Snssais;
}

void from_json(const nlohmann::json& j, SliceLoadLevelInformation& o) {
  j.at("loadLevelInformation").get_to(o.m_LoadLevelInformation);
  j.at("snssais").get_to(o.m_Snssais);
}

int32_t SliceLoadLevelInformation::getLoadLevelInformation() const {
  return m_LoadLevelInformation;
}
void SliceLoadLevelInformation::setLoadLevelInformation(int32_t const value) {
  m_LoadLevelInformation = value;
}
std::vector<oai::_3gpp::model::Snssai> SliceLoadLevelInformation::getSnssais()
    const {
  return m_Snssais;
}
void SliceLoadLevelInformation::setSnssais(
    std::vector<oai::_3gpp::model::Snssai> const& value) {
  m_Snssais = value;
}

}  // namespace oai::_3gpp::model
