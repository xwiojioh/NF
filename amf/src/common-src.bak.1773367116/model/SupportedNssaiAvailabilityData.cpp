/**
 * NSSF NSSAI Availability
 * NSSF NSSAI Availability Service.   © 2023, 3GPP Organizational Partners
 * (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 1.2.1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "SupportedNssaiAvailabilityData.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

SupportedNssaiAvailabilityData::SupportedNssaiAvailabilityData() {
  m_TaiListIsSet      = false;
  m_TaiRangeListIsSet = false;
  m_NsagInfosIsSet    = false;
}

void SupportedNssaiAvailabilityData::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool SupportedNssaiAvailabilityData::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool SupportedNssaiAvailabilityData::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success = true;
  const std::string _pathPrefix =
      pathPrefix.empty() ? "SupportedNssaiAvailabilityData" : pathPrefix;

  /* SupportedSnssaiList */ {
    const std::vector<oai::_3gpp::model::ExtSnssai>& value =
        m_SupportedSnssaiList;
    const std::string currentValuePath = _pathPrefix + ".supportedSnssaiList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::ExtSnssai& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success =
            value.validate(msg, currentValuePath + ".supportedSnssaiList") &&
            success;

        i++;
      }
    }
  }

  if (taiListIsSet()) {
    const std::vector<oai::_3gpp::model::Tai>& value = m_TaiList;
    const std::string currentValuePath               = _pathPrefix + ".taiList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::Tai& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success = value.validate(msg, currentValuePath + ".taiList") && success;

        i++;
      }
    }
  }

  if (taiRangeListIsSet()) {
    const std::vector<oai::_3gpp::model::TaiRange>& value = m_TaiRangeList;
    const std::string currentValuePath = _pathPrefix + ".taiRangeList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::TaiRange& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success =
            value.validate(msg, currentValuePath + ".taiRangeList") && success;

        i++;
      }
    }
  }

  if (nsagInfosIsSet()) {
    const std::vector<oai::_3gpp::model::NsagInfo>& value = m_NsagInfos;
    const std::string currentValuePath = _pathPrefix + ".nsagInfos";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::NsagInfo& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success =
            value.validate(msg, currentValuePath + ".nsagInfos") && success;

        i++;
      }
    }
  }

  return success;
}

bool SupportedNssaiAvailabilityData::operator==(
    const SupportedNssaiAvailabilityData& rhs) const {
  return

      (getTai() == rhs.getTai()) &&

      (getSupportedSnssaiList() == rhs.getSupportedSnssaiList()) &&

      ((!taiListIsSet() && !rhs.taiListIsSet()) ||
       (taiListIsSet() && rhs.taiListIsSet() &&
        getTaiList() == rhs.getTaiList())) &&

      ((!taiRangeListIsSet() && !rhs.taiRangeListIsSet()) ||
       (taiRangeListIsSet() && rhs.taiRangeListIsSet() &&
        getTaiRangeList() == rhs.getTaiRangeList())) &&

      ((!nsagInfosIsSet() && !rhs.nsagInfosIsSet()) ||
       (nsagInfosIsSet() && rhs.nsagInfosIsSet() &&
        getNsagInfos() == rhs.getNsagInfos()))

          ;
}

bool SupportedNssaiAvailabilityData::operator!=(
    const SupportedNssaiAvailabilityData& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const SupportedNssaiAvailabilityData& o) {
  j                        = nlohmann::json();
  j["tai"]                 = o.m_Tai;
  j["supportedSnssaiList"] = o.m_SupportedSnssaiList;
  if (o.taiListIsSet() || !o.m_TaiList.empty()) j["taiList"] = o.m_TaiList;
  if (o.taiRangeListIsSet() || !o.m_TaiRangeList.empty())
    j["taiRangeList"] = o.m_TaiRangeList;
  if (o.nsagInfosIsSet() || !o.m_NsagInfos.empty())
    j["nsagInfos"] = o.m_NsagInfos;
}

void from_json(const nlohmann::json& j, SupportedNssaiAvailabilityData& o) {
  j.at("tai").get_to(o.m_Tai);
  j.at("supportedSnssaiList").get_to(o.m_SupportedSnssaiList);
  if (j.find("taiList") != j.end()) {
    j.at("taiList").get_to(o.m_TaiList);
    o.m_TaiListIsSet = true;
  }
  if (j.find("taiRangeList") != j.end()) {
    j.at("taiRangeList").get_to(o.m_TaiRangeList);
    o.m_TaiRangeListIsSet = true;
  }
  if (j.find("nsagInfos") != j.end()) {
    j.at("nsagInfos").get_to(o.m_NsagInfos);
    o.m_NsagInfosIsSet = true;
  }
}

oai::_3gpp::model::Tai SupportedNssaiAvailabilityData::getTai() const {
  return m_Tai;
}
void SupportedNssaiAvailabilityData::setTai(
    oai::_3gpp::model::Tai const& value) {
  m_Tai = value;
}
std::vector<oai::_3gpp::model::ExtSnssai>
SupportedNssaiAvailabilityData::getSupportedSnssaiList() const {
  return m_SupportedSnssaiList;
}
void SupportedNssaiAvailabilityData::setSupportedSnssaiList(
    std::vector<oai::_3gpp::model::ExtSnssai> const& value) {
  m_SupportedSnssaiList = value;
}
std::vector<oai::_3gpp::model::Tai> SupportedNssaiAvailabilityData::getTaiList()
    const {
  return m_TaiList;
}
void SupportedNssaiAvailabilityData::setTaiList(
    std::vector<oai::_3gpp::model::Tai> const& value) {
  m_TaiList      = value;
  m_TaiListIsSet = true;
}
bool SupportedNssaiAvailabilityData::taiListIsSet() const {
  return m_TaiListIsSet;
}
void SupportedNssaiAvailabilityData::unsetTaiList() {
  m_TaiListIsSet = false;
}
std::vector<oai::_3gpp::model::TaiRange>
SupportedNssaiAvailabilityData::getTaiRangeList() const {
  return m_TaiRangeList;
}
void SupportedNssaiAvailabilityData::setTaiRangeList(
    std::vector<oai::_3gpp::model::TaiRange> const& value) {
  m_TaiRangeList      = value;
  m_TaiRangeListIsSet = true;
}
bool SupportedNssaiAvailabilityData::taiRangeListIsSet() const {
  return m_TaiRangeListIsSet;
}
void SupportedNssaiAvailabilityData::unsetTaiRangeList() {
  m_TaiRangeListIsSet = false;
}
std::vector<oai::_3gpp::model::NsagInfo>
SupportedNssaiAvailabilityData::getNsagInfos() const {
  return m_NsagInfos;
}
void SupportedNssaiAvailabilityData::setNsagInfos(
    std::vector<oai::_3gpp::model::NsagInfo> const& value) {
  m_NsagInfos      = value;
  m_NsagInfosIsSet = true;
}
bool SupportedNssaiAvailabilityData::nsagInfosIsSet() const {
  return m_NsagInfosIsSet;
}
void SupportedNssaiAvailabilityData::unsetNsagInfos() {
  m_NsagInfosIsSet = false;
}

}  // namespace oai::_3gpp::model
