/**
 * Nudm_SDM
 * Nudm Subscriber Data Management Service.   © 2023, 3GPP Organizational
 * Partners (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 2.2.4
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "ModificationNotification.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

ModificationNotification::ModificationNotification() {}

void ModificationNotification::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool ModificationNotification::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool ModificationNotification::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success = true;
  const std::string _pathPrefix =
      pathPrefix.empty() ? "ModificationNotification" : pathPrefix;

  /* NotifyItems */ {
    const std::vector<oai::_3gpp::model::NotifyItem>& value = m_NotifyItems;
    const std::string currentValuePath = _pathPrefix + ".notifyItems";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::NotifyItem& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success =
            value.validate(msg, currentValuePath + ".notifyItems") && success;

        i++;
      }
    }
  }

  return success;
}

bool ModificationNotification::operator==(
    const ModificationNotification& rhs) const {
  return

      (getNotifyItems() == rhs.getNotifyItems())

          ;
}

bool ModificationNotification::operator!=(
    const ModificationNotification& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const ModificationNotification& o) {
  j                = nlohmann::json();
  j["notifyItems"] = o.m_NotifyItems;
}

void from_json(const nlohmann::json& j, ModificationNotification& o) {
  j.at("notifyItems").get_to(o.m_NotifyItems);
}

std::vector<oai::_3gpp::model::NotifyItem>
ModificationNotification::getNotifyItems() const {
  return m_NotifyItems;
}
void ModificationNotification::setNotifyItems(
    std::vector<oai::_3gpp::model::NotifyItem> const& value) {
  m_NotifyItems = value;
}

}  // namespace oai::_3gpp::model
