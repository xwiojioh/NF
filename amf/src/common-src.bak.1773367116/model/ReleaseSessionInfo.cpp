/**
 * Namf_Communication
 * AMF Communication Service.   © 2023, 3GPP Organizational Partners (ARIB,
 * ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 1.2.4
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "ReleaseSessionInfo.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

ReleaseSessionInfo::ReleaseSessionInfo() {}

void ReleaseSessionInfo::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool ReleaseSessionInfo::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool ReleaseSessionInfo::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success = true;
  const std::string _pathPrefix =
      pathPrefix.empty() ? "ReleaseSessionInfo" : pathPrefix;

  /* ReleaseSessionList */ {
    const std::vector<int32_t>& value  = m_ReleaseSessionList;
    const std::string currentValuePath = _pathPrefix + ".releaseSessionList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const int32_t& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        if (value < 0) {
          success = false;
          msg << currentValuePath << ": must be greater than or equal to 0;";
        }
        if (value > 255) {
          success = false;
          msg << currentValuePath << ": must be less than or equal to 255;";
        }

        i++;
      }
    }
  }

  return success;
}

bool ReleaseSessionInfo::operator==(const ReleaseSessionInfo& rhs) const {
  return

      (getReleaseSessionList() == rhs.getReleaseSessionList()) &&

      (getReleaseCause() == rhs.getReleaseCause())

          ;
}

bool ReleaseSessionInfo::operator!=(const ReleaseSessionInfo& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const ReleaseSessionInfo& o) {
  j                       = nlohmann::json();
  j["releaseSessionList"] = o.m_ReleaseSessionList;
  j["releaseCause"]       = o.m_ReleaseCause;
}

void from_json(const nlohmann::json& j, ReleaseSessionInfo& o) {
  j.at("releaseSessionList").get_to(o.m_ReleaseSessionList);
  j.at("releaseCause").get_to(o.m_ReleaseCause);
}

std::vector<int32_t> ReleaseSessionInfo::getReleaseSessionList() const {
  return m_ReleaseSessionList;
}
void ReleaseSessionInfo::setReleaseSessionList(
    std::vector<int32_t> const value) {
  m_ReleaseSessionList = value;
}
oai::_3gpp::model::ReleaseCause ReleaseSessionInfo::getReleaseCause() const {
  return m_ReleaseCause;
}
void ReleaseSessionInfo::setReleaseCause(
    oai::_3gpp::model::ReleaseCause const& value) {
  m_ReleaseCause = value;
}

}  // namespace oai::_3gpp::model
