/**
 * Namf_Communication
 * AMF Communication Service.   © 2023, 3GPP Organizational Partners (ARIB,
 * ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 1.2.4
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "UncertaintyEllipsoid.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

UncertaintyEllipsoid::UncertaintyEllipsoid() {
  m_SemiMajor        = 0.0f;
  m_SemiMinor        = 0.0f;
  m_Vertical         = 0.0f;
  m_OrientationMajor = 0;
}

void UncertaintyEllipsoid::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool UncertaintyEllipsoid::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool UncertaintyEllipsoid::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success = true;
  const std::string _pathPrefix =
      pathPrefix.empty() ? "UncertaintyEllipsoid" : pathPrefix;

  /* SemiMajor */ {
    const float& value                 = m_SemiMajor;
    const std::string currentValuePath = _pathPrefix + ".semiMajor";

    if (value < static_cast<float>(0)) {
      success = false;
      msg << currentValuePath << ": must be greater than or equal to 0;";
    }
  }

  /* SemiMinor */ {
    const float& value                 = m_SemiMinor;
    const std::string currentValuePath = _pathPrefix + ".semiMinor";

    if (value < static_cast<float>(0)) {
      success = false;
      msg << currentValuePath << ": must be greater than or equal to 0;";
    }
  }

  /* Vertical */ {
    const float& value                 = m_Vertical;
    const std::string currentValuePath = _pathPrefix + ".vertical";

    if (value < static_cast<float>(0)) {
      success = false;
      msg << currentValuePath << ": must be greater than or equal to 0;";
    }
  }

  /* OrientationMajor */ {
    const int32_t& value               = m_OrientationMajor;
    const std::string currentValuePath = _pathPrefix + ".orientationMajor";

    if (value < 0) {
      success = false;
      msg << currentValuePath << ": must be greater than or equal to 0;";
    }
    if (value > 180) {
      success = false;
      msg << currentValuePath << ": must be less than or equal to 180;";
    }
  }

  return success;
}

bool UncertaintyEllipsoid::operator==(const UncertaintyEllipsoid& rhs) const {
  return

      (getSemiMajor() == rhs.getSemiMajor()) &&

      (getSemiMinor() == rhs.getSemiMinor()) &&

      (getVertical() == rhs.getVertical()) &&

      (getOrientationMajor() == rhs.getOrientationMajor())

          ;
}

bool UncertaintyEllipsoid::operator!=(const UncertaintyEllipsoid& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const UncertaintyEllipsoid& o) {
  j                     = nlohmann::json();
  j["semiMajor"]        = o.m_SemiMajor;
  j["semiMinor"]        = o.m_SemiMinor;
  j["vertical"]         = o.m_Vertical;
  j["orientationMajor"] = o.m_OrientationMajor;
}

void from_json(const nlohmann::json& j, UncertaintyEllipsoid& o) {
  j.at("semiMajor").get_to(o.m_SemiMajor);
  j.at("semiMinor").get_to(o.m_SemiMinor);
  j.at("vertical").get_to(o.m_Vertical);
  j.at("orientationMajor").get_to(o.m_OrientationMajor);
}

float UncertaintyEllipsoid::getSemiMajor() const {
  return m_SemiMajor;
}
void UncertaintyEllipsoid::setSemiMajor(float const value) {
  m_SemiMajor = value;
}
float UncertaintyEllipsoid::getSemiMinor() const {
  return m_SemiMinor;
}
void UncertaintyEllipsoid::setSemiMinor(float const value) {
  m_SemiMinor = value;
}
float UncertaintyEllipsoid::getVertical() const {
  return m_Vertical;
}
void UncertaintyEllipsoid::setVertical(float const value) {
  m_Vertical = value;
}
int32_t UncertaintyEllipsoid::getOrientationMajor() const {
  return m_OrientationMajor;
}
void UncertaintyEllipsoid::setOrientationMajor(int32_t const value) {
  m_OrientationMajor = value;
}

}  // namespace oai::_3gpp::model
