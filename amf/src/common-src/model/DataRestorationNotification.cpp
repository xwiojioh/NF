/**
 * Nudr_DataRepository API OpenAPI file
 * Unified Data Repository Service.   © 2023, 3GPP Organizational Partners
 * (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 2.2.4
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "DataRestorationNotification.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

DataRestorationNotification::DataRestorationNotification() {
  m_SupiRangesIsSet = false;
  m_GpsiRangesIsSet = false;
  m_ResetIdsIsSet   = false;
  m_SNssaiListIsSet = false;
  m_DnnListIsSet    = false;
  m_UdrGroupId      = "";
  m_UdrGroupIdIsSet = false;
}

void DataRestorationNotification::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool DataRestorationNotification::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool DataRestorationNotification::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success = true;
  const std::string _pathPrefix =
      pathPrefix.empty() ? "DataRestorationNotification" : pathPrefix;

  if (supiRangesIsSet()) {
    const std::vector<oai::_3gpp::model::SupiRange>& value = m_SupiRanges;
    const std::string currentValuePath = _pathPrefix + ".supiRanges";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::SupiRange& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success =
            value.validate(msg, currentValuePath + ".supiRanges") && success;

        i++;
      }
    }
  }

  if (gpsiRangesIsSet()) {
    const std::vector<oai::_3gpp::model::IdentityRange>& value = m_GpsiRanges;
    const std::string currentValuePath = _pathPrefix + ".gpsiRanges";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::IdentityRange& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success =
            value.validate(msg, currentValuePath + ".gpsiRanges") && success;

        i++;
      }
    }
  }

  if (resetIdsIsSet()) {
    const std::vector<std::string>& value = m_ResetIds;
    const std::string currentValuePath    = _pathPrefix + ".resetIds";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const std::string& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        i++;
      }
    }
  }

  if (sNssaiListIsSet()) {
    const std::vector<oai::_3gpp::model::Snssai>& value = m_SNssaiList;
    const std::string currentValuePath = _pathPrefix + ".sNssaiList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::Snssai& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success =
            value.validate(msg, currentValuePath + ".sNssaiList") && success;

        i++;
      }
    }
  }

  if (dnnListIsSet()) {
    const std::vector<std::string>& value = m_DnnList;
    const std::string currentValuePath    = _pathPrefix + ".dnnList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const std::string& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        i++;
      }
    }
  }

  return success;
}

bool DataRestorationNotification::operator==(
    const DataRestorationNotification& rhs) const {
  return

      ((!supiRangesIsSet() && !rhs.supiRangesIsSet()) ||
       (supiRangesIsSet() && rhs.supiRangesIsSet() &&
        getSupiRanges() == rhs.getSupiRanges())) &&

      ((!gpsiRangesIsSet() && !rhs.gpsiRangesIsSet()) ||
       (gpsiRangesIsSet() && rhs.gpsiRangesIsSet() &&
        getGpsiRanges() == rhs.getGpsiRanges())) &&

      ((!resetIdsIsSet() && !rhs.resetIdsIsSet()) ||
       (resetIdsIsSet() && rhs.resetIdsIsSet() &&
        getResetIds() == rhs.getResetIds())) &&

      ((!sNssaiListIsSet() && !rhs.sNssaiListIsSet()) ||
       (sNssaiListIsSet() && rhs.sNssaiListIsSet() &&
        getSNssaiList() == rhs.getSNssaiList())) &&

      ((!dnnListIsSet() && !rhs.dnnListIsSet()) ||
       (dnnListIsSet() && rhs.dnnListIsSet() &&
        getDnnList() == rhs.getDnnList())) &&

      ((!udrGroupIdIsSet() && !rhs.udrGroupIdIsSet()) ||
       (udrGroupIdIsSet() && rhs.udrGroupIdIsSet() &&
        getUdrGroupId() == rhs.getUdrGroupId()))

          ;
}

bool DataRestorationNotification::operator!=(
    const DataRestorationNotification& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const DataRestorationNotification& o) {
  j = nlohmann::json();
  if (o.supiRangesIsSet() || !o.m_SupiRanges.empty())
    j["supiRanges"] = o.m_SupiRanges;
  if (o.gpsiRangesIsSet() || !o.m_GpsiRanges.empty())
    j["gpsiRanges"] = o.m_GpsiRanges;
  if (o.resetIdsIsSet() || !o.m_ResetIds.empty()) j["resetIds"] = o.m_ResetIds;
  if (o.sNssaiListIsSet() || !o.m_SNssaiList.empty())
    j["sNssaiList"] = o.m_SNssaiList;
  if (o.dnnListIsSet() || !o.m_DnnList.empty()) j["dnnList"] = o.m_DnnList;
  if (o.udrGroupIdIsSet()) j["udrGroupId"] = o.m_UdrGroupId;
}

void from_json(const nlohmann::json& j, DataRestorationNotification& o) {
  if (j.find("supiRanges") != j.end()) {
    j.at("supiRanges").get_to(o.m_SupiRanges);
    o.m_SupiRangesIsSet = true;
  }
  if (j.find("gpsiRanges") != j.end()) {
    j.at("gpsiRanges").get_to(o.m_GpsiRanges);
    o.m_GpsiRangesIsSet = true;
  }
  if (j.find("resetIds") != j.end()) {
    j.at("resetIds").get_to(o.m_ResetIds);
    o.m_ResetIdsIsSet = true;
  }
  if (j.find("sNssaiList") != j.end()) {
    j.at("sNssaiList").get_to(o.m_SNssaiList);
    o.m_SNssaiListIsSet = true;
  }
  if (j.find("dnnList") != j.end()) {
    j.at("dnnList").get_to(o.m_DnnList);
    o.m_DnnListIsSet = true;
  }
  if (j.find("udrGroupId") != j.end()) {
    j.at("udrGroupId").get_to(o.m_UdrGroupId);
    o.m_UdrGroupIdIsSet = true;
  }
}

std::vector<oai::_3gpp::model::SupiRange>
DataRestorationNotification::getSupiRanges() const {
  return m_SupiRanges;
}
void DataRestorationNotification::setSupiRanges(
    std::vector<oai::_3gpp::model::SupiRange> const& value) {
  m_SupiRanges      = value;
  m_SupiRangesIsSet = true;
}
bool DataRestorationNotification::supiRangesIsSet() const {
  return m_SupiRangesIsSet;
}
void DataRestorationNotification::unsetSupiRanges() {
  m_SupiRangesIsSet = false;
}
std::vector<oai::_3gpp::model::IdentityRange>
DataRestorationNotification::getGpsiRanges() const {
  return m_GpsiRanges;
}
void DataRestorationNotification::setGpsiRanges(
    std::vector<oai::_3gpp::model::IdentityRange> const& value) {
  m_GpsiRanges      = value;
  m_GpsiRangesIsSet = true;
}
bool DataRestorationNotification::gpsiRangesIsSet() const {
  return m_GpsiRangesIsSet;
}
void DataRestorationNotification::unsetGpsiRanges() {
  m_GpsiRangesIsSet = false;
}
std::vector<std::string> DataRestorationNotification::getResetIds() const {
  return m_ResetIds;
}
void DataRestorationNotification::setResetIds(
    std::vector<std::string> const& value) {
  m_ResetIds      = value;
  m_ResetIdsIsSet = true;
}
bool DataRestorationNotification::resetIdsIsSet() const {
  return m_ResetIdsIsSet;
}
void DataRestorationNotification::unsetResetIds() {
  m_ResetIdsIsSet = false;
}
std::vector<oai::_3gpp::model::Snssai>
DataRestorationNotification::getSNssaiList() const {
  return m_SNssaiList;
}
void DataRestorationNotification::setSNssaiList(
    std::vector<oai::_3gpp::model::Snssai> const& value) {
  m_SNssaiList      = value;
  m_SNssaiListIsSet = true;
}
bool DataRestorationNotification::sNssaiListIsSet() const {
  return m_SNssaiListIsSet;
}
void DataRestorationNotification::unsetSNssaiList() {
  m_SNssaiListIsSet = false;
}
std::vector<std::string> DataRestorationNotification::getDnnList() const {
  return m_DnnList;
}
void DataRestorationNotification::setDnnList(
    std::vector<std::string> const& value) {
  m_DnnList      = value;
  m_DnnListIsSet = true;
}
bool DataRestorationNotification::dnnListIsSet() const {
  return m_DnnListIsSet;
}
void DataRestorationNotification::unsetDnnList() {
  m_DnnListIsSet = false;
}
std::string DataRestorationNotification::getUdrGroupId() const {
  return m_UdrGroupId;
}
void DataRestorationNotification::setUdrGroupId(std::string const& value) {
  m_UdrGroupId      = value;
  m_UdrGroupIdIsSet = true;
}
bool DataRestorationNotification::udrGroupIdIsSet() const {
  return m_UdrGroupIdIsSet;
}
void DataRestorationNotification::unsetUdrGroupId() {
  m_UdrGroupIdIsSet = false;
}

}  // namespace oai::_3gpp::model
