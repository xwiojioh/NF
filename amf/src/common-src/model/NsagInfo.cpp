/**
 * NSSF NS Selection
 * NSSF Network Slice Selection Service.   © 2024, 3GPP Organizational Partners
 * (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 2.2.2
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "NsagInfo.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

NsagInfo::NsagInfo() {
  m_TaiListIsSet      = false;
  m_TaiRangeListIsSet = false;
}

void NsagInfo::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool NsagInfo::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool NsagInfo::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success                  = true;
  const std::string _pathPrefix = pathPrefix.empty() ? "NsagInfo" : pathPrefix;

  /* NsagIds */ {
    const std::vector<int32_t>& value  = m_NsagIds;
    const std::string currentValuePath = _pathPrefix + ".nsagIds";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const int32_t& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        i++;
      }
    }
  }

  /* SnssaiList */ {
    const std::vector<oai::_3gpp::model::Snssai>& value = m_SnssaiList;
    const std::string currentValuePath = _pathPrefix + ".snssaiList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::Snssai& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success =
            value.validate(msg, currentValuePath + ".snssaiList") && success;

        i++;
      }
    }
  }

  if (taiListIsSet()) {
    const std::vector<oai::_3gpp::model::Tai>& value = m_TaiList;
    const std::string currentValuePath               = _pathPrefix + ".taiList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::Tai& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success = value.validate(msg, currentValuePath + ".taiList") && success;

        i++;
      }
    }
  }

  if (taiRangeListIsSet()) {
    const std::vector<oai::_3gpp::model::TaiRange>& value = m_TaiRangeList;
    const std::string currentValuePath = _pathPrefix + ".taiRangeList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::TaiRange& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success =
            value.validate(msg, currentValuePath + ".taiRangeList") && success;

        i++;
      }
    }
  }

  return success;
}

bool NsagInfo::operator==(const NsagInfo& rhs) const {
  return

      (getNsagIds() == rhs.getNsagIds()) &&

      (getSnssaiList() == rhs.getSnssaiList()) &&

      ((!taiListIsSet() && !rhs.taiListIsSet()) ||
       (taiListIsSet() && rhs.taiListIsSet() &&
        getTaiList() == rhs.getTaiList())) &&

      ((!taiRangeListIsSet() && !rhs.taiRangeListIsSet()) ||
       (taiRangeListIsSet() && rhs.taiRangeListIsSet() &&
        getTaiRangeList() == rhs.getTaiRangeList()))

          ;
}

bool NsagInfo::operator!=(const NsagInfo& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const NsagInfo& o) {
  j               = nlohmann::json();
  j["nsagIds"]    = o.m_NsagIds;
  j["snssaiList"] = o.m_SnssaiList;
  if (o.taiListIsSet() || !o.m_TaiList.empty()) j["taiList"] = o.m_TaiList;
  if (o.taiRangeListIsSet() || !o.m_TaiRangeList.empty())
    j["taiRangeList"] = o.m_TaiRangeList;
}

void from_json(const nlohmann::json& j, NsagInfo& o) {
  j.at("nsagIds").get_to(o.m_NsagIds);
  j.at("snssaiList").get_to(o.m_SnssaiList);
  if (j.find("taiList") != j.end()) {
    j.at("taiList").get_to(o.m_TaiList);
    o.m_TaiListIsSet = true;
  }
  if (j.find("taiRangeList") != j.end()) {
    j.at("taiRangeList").get_to(o.m_TaiRangeList);
    o.m_TaiRangeListIsSet = true;
  }
}

std::vector<int32_t> NsagInfo::getNsagIds() const {
  return m_NsagIds;
}
void NsagInfo::setNsagIds(std::vector<int32_t> const value) {
  m_NsagIds = value;
}
std::vector<oai::_3gpp::model::Snssai> NsagInfo::getSnssaiList() const {
  return m_SnssaiList;
}
void NsagInfo::setSnssaiList(
    std::vector<oai::_3gpp::model::Snssai> const& value) {
  m_SnssaiList = value;
}
std::vector<oai::_3gpp::model::Tai> NsagInfo::getTaiList() const {
  return m_TaiList;
}
void NsagInfo::setTaiList(std::vector<oai::_3gpp::model::Tai> const& value) {
  m_TaiList      = value;
  m_TaiListIsSet = true;
}
bool NsagInfo::taiListIsSet() const {
  return m_TaiListIsSet;
}
void NsagInfo::unsetTaiList() {
  m_TaiListIsSet = false;
}
std::vector<oai::_3gpp::model::TaiRange> NsagInfo::getTaiRangeList() const {
  return m_TaiRangeList;
}
void NsagInfo::setTaiRangeList(
    std::vector<oai::_3gpp::model::TaiRange> const& value) {
  m_TaiRangeList      = value;
  m_TaiRangeListIsSet = true;
}
bool NsagInfo::taiRangeListIsSet() const {
  return m_TaiRangeListIsSet;
}
void NsagInfo::unsetTaiRangeList() {
  m_TaiRangeListIsSet = false;
}

}  // namespace oai::_3gpp::model
