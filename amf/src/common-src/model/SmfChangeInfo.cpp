/**
 * Namf_Communication
 * AMF Communication Service.   © 2023, 3GPP Organizational Partners (ARIB,
 * ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 1.2.4
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "SmfChangeInfo.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

SmfChangeInfo::SmfChangeInfo() {}

void SmfChangeInfo::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool SmfChangeInfo::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool SmfChangeInfo::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success = true;
  const std::string _pathPrefix =
      pathPrefix.empty() ? "SmfChangeInfo" : pathPrefix;

  /* PduSessionIdList */ {
    const std::vector<int32_t>& value  = m_PduSessionIdList;
    const std::string currentValuePath = _pathPrefix + ".pduSessionIdList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const int32_t& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        if (value < 0) {
          success = false;
          msg << currentValuePath << ": must be greater than or equal to 0;";
        }
        if (value > 255) {
          success = false;
          msg << currentValuePath << ": must be less than or equal to 255;";
        }

        i++;
      }
    }
  }

  return success;
}

bool SmfChangeInfo::operator==(const SmfChangeInfo& rhs) const {
  return

      (getPduSessionIdList() == rhs.getPduSessionIdList()) &&

      (getSmfChangeInd() == rhs.getSmfChangeInd())

          ;
}

bool SmfChangeInfo::operator!=(const SmfChangeInfo& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const SmfChangeInfo& o) {
  j                     = nlohmann::json();
  j["pduSessionIdList"] = o.m_PduSessionIdList;
  j["smfChangeInd"]     = o.m_SmfChangeInd;
}

void from_json(const nlohmann::json& j, SmfChangeInfo& o) {
  j.at("pduSessionIdList").get_to(o.m_PduSessionIdList);
  j.at("smfChangeInd").get_to(o.m_SmfChangeInd);
}

std::vector<int32_t> SmfChangeInfo::getPduSessionIdList() const {
  return m_PduSessionIdList;
}
void SmfChangeInfo::setPduSessionIdList(std::vector<int32_t> const value) {
  m_PduSessionIdList = value;
}
oai::_3gpp::model::SmfChangeIndication SmfChangeInfo::getSmfChangeInd() const {
  return m_SmfChangeInd;
}
void SmfChangeInfo::setSmfChangeInd(
    oai::_3gpp::model::SmfChangeIndication const& value) {
  m_SmfChangeInd = value;
}

}  // namespace oai::_3gpp::model
