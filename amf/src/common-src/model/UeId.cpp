/**
 * Nudr_DataRepository API OpenAPI file
 * Unified Data Repository Service.   © 2023, 3GPP Organizational Partners
 * (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 2.2.4
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "UeId.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

UeId::UeId() {
  m_Supi          = "";
  m_GpsiListIsSet = false;
}

void UeId::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool UeId::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool UeId::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success                  = true;
  const std::string _pathPrefix = pathPrefix.empty() ? "UeId" : pathPrefix;

  /* Supi */ {
    const std::string& value           = m_Supi;
    const std::string currentValuePath = _pathPrefix + ".supi";
  }

  if (gpsiListIsSet()) {
    const std::vector<std::string>& value = m_GpsiList;
    const std::string currentValuePath    = _pathPrefix + ".gpsiList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const std::string& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        i++;
      }
    }
  }

  return success;
}

bool UeId::operator==(const UeId& rhs) const {
  return

      (getSupi() == rhs.getSupi()) &&

      ((!gpsiListIsSet() && !rhs.gpsiListIsSet()) ||
       (gpsiListIsSet() && rhs.gpsiListIsSet() &&
        getGpsiList() == rhs.getGpsiList()))

          ;
}

bool UeId::operator!=(const UeId& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const UeId& o) {
  j         = nlohmann::json();
  j["supi"] = o.m_Supi;
  if (o.gpsiListIsSet() || !o.m_GpsiList.empty()) j["gpsiList"] = o.m_GpsiList;
}

void from_json(const nlohmann::json& j, UeId& o) {
  j.at("supi").get_to(o.m_Supi);
  if (j.find("gpsiList") != j.end()) {
    j.at("gpsiList").get_to(o.m_GpsiList);
    o.m_GpsiListIsSet = true;
  }
}

std::string UeId::getSupi() const {
  return m_Supi;
}
void UeId::setSupi(std::string const& value) {
  m_Supi = value;
}
std::vector<std::string> UeId::getGpsiList() const {
  return m_GpsiList;
}
void UeId::setGpsiList(std::vector<std::string> const& value) {
  m_GpsiList      = value;
  m_GpsiListIsSet = true;
}
bool UeId::gpsiListIsSet() const {
  return m_GpsiListIsSet;
}
void UeId::unsetGpsiList() {
  m_GpsiListIsSet = false;
}

}  // namespace oai::_3gpp::model
