/**
 * Namf_Communication
 * AMF Communication Service.   © 2023, 3GPP Organizational Partners (ARIB,
 * ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 1.2.4
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "SmcceUeList.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

SmcceUeList::SmcceUeList() {
  m_HighLevelIsSet   = false;
  m_MediumLevelIsSet = false;
  m_LowLevelIsSet    = false;
}

void SmcceUeList::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool SmcceUeList::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool SmcceUeList::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success = true;
  const std::string _pathPrefix =
      pathPrefix.empty() ? "SmcceUeList" : pathPrefix;

  if (highLevelIsSet()) {
    const std::vector<std::string>& value = m_HighLevel;
    const std::string currentValuePath    = _pathPrefix + ".highLevel";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const std::string& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        i++;
      }
    }
  }

  if (mediumLevelIsSet()) {
    const std::vector<std::string>& value = m_MediumLevel;
    const std::string currentValuePath    = _pathPrefix + ".mediumLevel";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const std::string& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        i++;
      }
    }
  }

  if (lowLevelIsSet()) {
    const std::vector<std::string>& value = m_LowLevel;
    const std::string currentValuePath    = _pathPrefix + ".lowLevel";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const std::string& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        i++;
      }
    }
  }

  return success;
}

bool SmcceUeList::operator==(const SmcceUeList& rhs) const {
  return

      ((!highLevelIsSet() && !rhs.highLevelIsSet()) ||
       (highLevelIsSet() && rhs.highLevelIsSet() &&
        getHighLevel() == rhs.getHighLevel())) &&

      ((!mediumLevelIsSet() && !rhs.mediumLevelIsSet()) ||
       (mediumLevelIsSet() && rhs.mediumLevelIsSet() &&
        getMediumLevel() == rhs.getMediumLevel())) &&

      ((!lowLevelIsSet() && !rhs.lowLevelIsSet()) ||
       (lowLevelIsSet() && rhs.lowLevelIsSet() &&
        getLowLevel() == rhs.getLowLevel()))

          ;
}

bool SmcceUeList::operator!=(const SmcceUeList& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const SmcceUeList& o) {
  j = nlohmann::json();
  if (o.highLevelIsSet() || !o.m_HighLevel.empty())
    j["highLevel"] = o.m_HighLevel;
  if (o.mediumLevelIsSet() || !o.m_MediumLevel.empty())
    j["mediumLevel"] = o.m_MediumLevel;
  if (o.lowLevelIsSet() || !o.m_LowLevel.empty()) j["lowLevel"] = o.m_LowLevel;
}

void from_json(const nlohmann::json& j, SmcceUeList& o) {
  if (j.find("highLevel") != j.end()) {
    j.at("highLevel").get_to(o.m_HighLevel);
    o.m_HighLevelIsSet = true;
  }
  if (j.find("mediumLevel") != j.end()) {
    j.at("mediumLevel").get_to(o.m_MediumLevel);
    o.m_MediumLevelIsSet = true;
  }
  if (j.find("lowLevel") != j.end()) {
    j.at("lowLevel").get_to(o.m_LowLevel);
    o.m_LowLevelIsSet = true;
  }
}

std::vector<std::string> SmcceUeList::getHighLevel() const {
  return m_HighLevel;
}
void SmcceUeList::setHighLevel(std::vector<std::string> const& value) {
  m_HighLevel      = value;
  m_HighLevelIsSet = true;
}
bool SmcceUeList::highLevelIsSet() const {
  return m_HighLevelIsSet;
}
void SmcceUeList::unsetHighLevel() {
  m_HighLevelIsSet = false;
}
std::vector<std::string> SmcceUeList::getMediumLevel() const {
  return m_MediumLevel;
}
void SmcceUeList::setMediumLevel(std::vector<std::string> const& value) {
  m_MediumLevel      = value;
  m_MediumLevelIsSet = true;
}
bool SmcceUeList::mediumLevelIsSet() const {
  return m_MediumLevelIsSet;
}
void SmcceUeList::unsetMediumLevel() {
  m_MediumLevelIsSet = false;
}
std::vector<std::string> SmcceUeList::getLowLevel() const {
  return m_LowLevel;
}
void SmcceUeList::setLowLevel(std::vector<std::string> const& value) {
  m_LowLevel      = value;
  m_LowLevelIsSet = true;
}
bool SmcceUeList::lowLevelIsSet() const {
  return m_LowLevelIsSet;
}
void SmcceUeList::unsetLowLevel() {
  m_LowLevelIsSet = false;
}

}  // namespace oai::_3gpp::model
