/**
 * Namf_Communication
 * AMF Communication Service.   © 2023, 3GPP Organizational Partners (ARIB,
 * ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 1.2.4
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "AssignedEbiData.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

AssignedEbiData::AssignedEbiData() {
  m_PduSessionId         = 0;
  m_FailedArpListIsSet   = false;
  m_ReleasedEbiListIsSet = false;
  m_ModifiedEbiListIsSet = false;
}

void AssignedEbiData::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool AssignedEbiData::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool AssignedEbiData::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success = true;
  const std::string _pathPrefix =
      pathPrefix.empty() ? "AssignedEbiData" : pathPrefix;

  /* PduSessionId */ {
    const int32_t& value               = m_PduSessionId;
    const std::string currentValuePath = _pathPrefix + ".pduSessionId";

    if (value < 0) {
      success = false;
      msg << currentValuePath << ": must be greater than or equal to 0;";
    }
    if (value > 255) {
      success = false;
      msg << currentValuePath << ": must be less than or equal to 255;";
    }
  }

  /* AssignedEbiList */ {
    const std::vector<oai::_3gpp::model::EbiArpMapping>& value =
        m_AssignedEbiList;
    const std::string currentValuePath = _pathPrefix + ".assignedEbiList";

    if (value.size() < 0) {
      success = false;
      msg << currentValuePath << ": must have at least 0 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::EbiArpMapping& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success = value.validate(msg, currentValuePath + ".assignedEbiList") &&
                  success;

        i++;
      }
    }
  }

  if (failedArpListIsSet()) {
    const std::vector<oai::_3gpp::model::Arp>& value = m_FailedArpList;
    const std::string currentValuePath = _pathPrefix + ".failedArpList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const oai::_3gpp::model::Arp& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        success =
            value.validate(msg, currentValuePath + ".failedArpList") && success;

        i++;
      }
    }
  }

  if (releasedEbiListIsSet()) {
    const std::vector<int32_t>& value  = m_ReleasedEbiList;
    const std::string currentValuePath = _pathPrefix + ".releasedEbiList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const int32_t& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        if (value < 0) {
          success = false;
          msg << currentValuePath << ": must be greater than or equal to 0;";
        }
        if (value > 15) {
          success = false;
          msg << currentValuePath << ": must be less than or equal to 15;";
        }

        i++;
      }
    }
  }

  if (modifiedEbiListIsSet()) {
    const std::vector<int32_t>& value  = m_ModifiedEbiList;
    const std::string currentValuePath = _pathPrefix + ".modifiedEbiList";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const int32_t& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        if (value < 0) {
          success = false;
          msg << currentValuePath << ": must be greater than or equal to 0;";
        }
        if (value > 15) {
          success = false;
          msg << currentValuePath << ": must be less than or equal to 15;";
        }

        i++;
      }
    }
  }

  return success;
}

bool AssignedEbiData::operator==(const AssignedEbiData& rhs) const {
  return

      (getPduSessionId() == rhs.getPduSessionId()) &&

      (getAssignedEbiList() == rhs.getAssignedEbiList()) &&

      ((!failedArpListIsSet() && !rhs.failedArpListIsSet()) ||
       (failedArpListIsSet() && rhs.failedArpListIsSet() &&
        getFailedArpList() == rhs.getFailedArpList())) &&

      ((!releasedEbiListIsSet() && !rhs.releasedEbiListIsSet()) ||
       (releasedEbiListIsSet() && rhs.releasedEbiListIsSet() &&
        getReleasedEbiList() == rhs.getReleasedEbiList())) &&

      ((!modifiedEbiListIsSet() && !rhs.modifiedEbiListIsSet()) ||
       (modifiedEbiListIsSet() && rhs.modifiedEbiListIsSet() &&
        getModifiedEbiList() == rhs.getModifiedEbiList()))

          ;
}

bool AssignedEbiData::operator!=(const AssignedEbiData& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const AssignedEbiData& o) {
  j                    = nlohmann::json();
  j["pduSessionId"]    = o.m_PduSessionId;
  j["assignedEbiList"] = o.m_AssignedEbiList;
  if (o.failedArpListIsSet() || !o.m_FailedArpList.empty())
    j["failedArpList"] = o.m_FailedArpList;
  if (o.releasedEbiListIsSet() || !o.m_ReleasedEbiList.empty())
    j["releasedEbiList"] = o.m_ReleasedEbiList;
  if (o.modifiedEbiListIsSet() || !o.m_ModifiedEbiList.empty())
    j["modifiedEbiList"] = o.m_ModifiedEbiList;
}

void from_json(const nlohmann::json& j, AssignedEbiData& o) {
  j.at("pduSessionId").get_to(o.m_PduSessionId);
  j.at("assignedEbiList").get_to(o.m_AssignedEbiList);
  if (j.find("failedArpList") != j.end()) {
    j.at("failedArpList").get_to(o.m_FailedArpList);
    o.m_FailedArpListIsSet = true;
  }
  if (j.find("releasedEbiList") != j.end()) {
    j.at("releasedEbiList").get_to(o.m_ReleasedEbiList);
    o.m_ReleasedEbiListIsSet = true;
  }
  if (j.find("modifiedEbiList") != j.end()) {
    j.at("modifiedEbiList").get_to(o.m_ModifiedEbiList);
    o.m_ModifiedEbiListIsSet = true;
  }
}

int32_t AssignedEbiData::getPduSessionId() const {
  return m_PduSessionId;
}
void AssignedEbiData::setPduSessionId(int32_t const value) {
  m_PduSessionId = value;
}
std::vector<oai::_3gpp::model::EbiArpMapping>
AssignedEbiData::getAssignedEbiList() const {
  return m_AssignedEbiList;
}
void AssignedEbiData::setAssignedEbiList(
    std::vector<oai::_3gpp::model::EbiArpMapping> const& value) {
  m_AssignedEbiList = value;
}
std::vector<oai::_3gpp::model::Arp> AssignedEbiData::getFailedArpList() const {
  return m_FailedArpList;
}
void AssignedEbiData::setFailedArpList(
    std::vector<oai::_3gpp::model::Arp> const& value) {
  m_FailedArpList      = value;
  m_FailedArpListIsSet = true;
}
bool AssignedEbiData::failedArpListIsSet() const {
  return m_FailedArpListIsSet;
}
void AssignedEbiData::unsetFailedArpList() {
  m_FailedArpListIsSet = false;
}
std::vector<int32_t> AssignedEbiData::getReleasedEbiList() const {
  return m_ReleasedEbiList;
}
void AssignedEbiData::setReleasedEbiList(std::vector<int32_t> const value) {
  m_ReleasedEbiList      = value;
  m_ReleasedEbiListIsSet = true;
}
bool AssignedEbiData::releasedEbiListIsSet() const {
  return m_ReleasedEbiListIsSet;
}
void AssignedEbiData::unsetReleasedEbiList() {
  m_ReleasedEbiListIsSet = false;
}
std::vector<int32_t> AssignedEbiData::getModifiedEbiList() const {
  return m_ModifiedEbiList;
}
void AssignedEbiData::setModifiedEbiList(std::vector<int32_t> const value) {
  m_ModifiedEbiList      = value;
  m_ModifiedEbiListIsSet = true;
}
bool AssignedEbiData::modifiedEbiListIsSet() const {
  return m_ModifiedEbiListIsSet;
}
void AssignedEbiData::unsetModifiedEbiList() {
  m_ModifiedEbiListIsSet = false;
}

}  // namespace oai::_3gpp::model
