/**
 * Nnef_EventExposure
 * NEF Event Exposure Service.   © 2022 , 3GPP Organizational Partners (ARIB,
 * ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * The version of the OpenAPI document: 1.2.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
 * the class manually.
 */

#include "MsConsumptionCollection.h"
#include "Helpers.h"

#include <sstream>

namespace oai::_3gpp::model {

MsConsumptionCollection::MsConsumptionCollection() {}

void MsConsumptionCollection::validate() const {
  std::stringstream msg;
  if (!validate(msg)) {
    throw oai::_3gpp::model::helpers::ValidationException(msg.str());
  }
}

bool MsConsumptionCollection::validate(std::stringstream& msg) const {
  return validate(msg, "");
}

bool MsConsumptionCollection::validate(
    std::stringstream& msg, const std::string& pathPrefix) const {
  bool success = true;
  const std::string _pathPrefix =
      pathPrefix.empty() ? "MsConsumptionCollection" : pathPrefix;

  /* MsConsumps */ {
    const std::vector<std::string>& value = m_MsConsumps;
    const std::string currentValuePath    = _pathPrefix + ".msConsumps";

    if (value.size() < 1) {
      success = false;
      msg << currentValuePath << ": must have at least 1 elements;";
    }
    {  // Recursive validation of array elements
      const std::string oldValuePath = currentValuePath;
      int i                          = 0;
      for (const std::string& value : value) {
        const std::string currentValuePath =
            oldValuePath + "[" + std::to_string(i) + "]";

        i++;
      }
    }
  }

  return success;
}

bool MsConsumptionCollection::operator==(
    const MsConsumptionCollection& rhs) const {
  return

      (getMsConsumps() == rhs.getMsConsumps())

          ;
}

bool MsConsumptionCollection::operator!=(
    const MsConsumptionCollection& rhs) const {
  return !(*this == rhs);
}

void to_json(nlohmann::json& j, const MsConsumptionCollection& o) {
  j               = nlohmann::json();
  j["msConsumps"] = o.m_MsConsumps;
}

void from_json(const nlohmann::json& j, MsConsumptionCollection& o) {
  j.at("msConsumps").get_to(o.m_MsConsumps);
}

std::vector<std::string> MsConsumptionCollection::getMsConsumps() const {
  return m_MsConsumps;
}
void MsConsumptionCollection::setMsConsumps(
    std::vector<std::string> const& value) {
  m_MsConsumps = value;
}

}  // namespace oai::_3gpp::model
